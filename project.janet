(declare-project
 :name "rubish" # required
 :description "junk-drawer + jaylib + juice = environment for game jams"

 # Optional urls to git repositories that contain required artifacts.
 :dependencies ["https://github.com/AlecTroemel/junk-drawer"
                "https://github.com/janet-lang/jaylib.git"])
